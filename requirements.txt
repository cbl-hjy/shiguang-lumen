# 拾光 Lumen · 后端依赖
# 安装：python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
# 版本锁定为开发环境实测通过的值；换机复现以本文件为准。
# 嵌入默认走 Ollama HTTP（见 README），因此**无需本地 torch**。

# ── Web 框架与流式 ──
fastapi==0.141.1
uvicorn==0.52.1
sse-starlette==3.4.8          # SSE 流式（对话 / 会议事件流）
python-multipart==0.0.32      # 文件上传
python-dotenv==1.2.2          # .env 加载
PyYAML==6.0.3                 # Prompt / 剧本外置

# ── Agent 底座 ──
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic-ai==2.27.0
openai==2.53.0                # DeepSeek provider（OpenAI 兼容协议）
anthropic==0.121.0            # pydantic-ai 传递依赖，勿删

# ── 可观测与协议 ──
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
mcp==2.1.1                    # MCP 服务端
fastmcp==3.4.6

# ── 记忆 / 检索 / 向量 ──
chromadb==1.5.9               # 向量库
ollama==0.6.2                 # 本地嵌入服务 HTTP 客户端
tiktoken==0.13.0              # token 计量
tenacity==9.1.4               # 重试

# ── 知识库（LlamaIndex）──
llama-index-core==0.14.23
llama-index-llms-openai-like==0.7.2
llama-index-embeddings-ollama==0.9.0
llama-index-vector-stores-chroma==0.5.5

# ── 沙箱 / 数据分析 ──
numpy==2.5.2
pandas==3.0.5                 # 沙箱内动态 import（静态分析抓不到，手动补）
RestrictedPython==8.4         # 语言约束层

# ── 文档 / OCR / 图片 ──
pymupdf==1.28.2               # PDF 解析
markitdown==0.1.7             # 多格式转 Markdown
rapidocr-onnxruntime==1.2.3   # OCR
beautifulsoup4==4.15.0        # 网页解析
pillow==12.3.0
zai-sdk==0.2.3                # 智谱：识图（GLM-4.6V-Flash）/ 生图（CogView）
requests==2.34.2

# ── 数据库 ──
SQLAlchemy==2.0.51
aiosqlite==0.22.1

# ── 多用户账号 ──
pwdlib[argon2]==0.3.1         # 密码哈希（argon2id）
PyJWT==2.13.0                 # 会话令牌

# ── 可选（不装则对应功能不可用，其余不受影响）──
# Jinja2==3.1.6                          # 模板渲染
# sentence-transformers==5.7.0           # 本地嵌入备用（默认走 Ollama，不装亦可）
# torch==2.13.0                          # 仅 sentence-transformers 需要（体积大，按需安装）
# llama-index-embeddings-huggingface==0.7.0   # 本地 HF 嵌入备用
